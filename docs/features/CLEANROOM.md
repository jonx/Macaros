# Clean-room process — how we use GPL references without contaminating AROS

> Drafted 2026-06-24 · Applies to every `spec.md` under `docs/features/*/`

## Why this exists

The best worked examples of "bridge an Amiga-like to macOS" live in GPL-2.0 projects
— the **UAE family** (WinUAE, FS-UAE, Amiberry, E-UAE/Janus-UAE) and **vAmiga**. AROS
is **APL / LGPL-2.1**. We cannot import, paraphrase, or structurally mirror GPL code
into AROS: that would impose GPL on those files and is a licence violation if we then
ship under AROS terms. But we *can* learn from them — legally — through a clean-room
(Chinese-wall) split. This doc is the firewall.

> Not legal advice. This is an engineering process to minimise copyright risk; the
> project owner confirms the licence strategy before anything ships.

## The two roles

**Role A — Spec Author ("sees everything").** May read the GPL references, the public
API docs, the AROS tree, and our own spikes. Produces a **functional specification**:
interface contracts, required observable behaviour, algorithms described as
*requirements in our own words*, and acceptance tests. Records **provenance** for every
requirement. Writes the `spec.md`. *(This is the role the reading-agent plays.)*

**Role B — Implementer ("never saw the GPL code").** Has **not** read UAE/vAmiga/any
GPL reference. Implements **solely** from (1) Role A's `spec.md` and (2) the approved
public sources below. Produces the AROS-licensed code. *(A fresh agent with no GPL
context in its window.)*

The wall is between B and the GPL code. A's spec is the only thing that crosses it, and
a spec carries *facts and contracts*, never *expression*.

## What a spec MAY contain

- **External interface signatures** — Apple framework APIs (AppKit/Metal/CoreAudio/…),
  AROS in-tree headers (APL/LGPL — already ours to use), POSIX. These are interfaces,
  not the GPL code's expression.
- **Data layouts dictated by an external standard** — IFF FTXT, AmigaOS hunk, `fd_set`,
  the AHI sub-driver struct. Mandated by the format/API, not authored by UAE.
- **Algorithms as requirements**, in our own prose or neutral pseudocode — *what* must
  happen and *why*, derived from an independent constraint.
- **Acceptance tests** — the unattended-loop markers and pixel/byte/RMS asserts.
- **Provenance tags** (below) on every non-trivial requirement.

## What a spec MUST NOT contain

- Verbatim or paraphrased GPL source, or its comments.
- A GPL file's identifier names, call sequence, or file/function layout used as a
  template. (Names dictated by an *external* API — `nextDrawable`, `WaitSelect` — are
  fine; names that are the GPL author's invention are not.)
- Any requirement whose *only* justification is "because UAE does it that way." If a
  behaviour can't be re-derived from a public source or our own constraints, it doesn't
  belong in the spec.

## Provenance tags (use them in every spec)

Tag each substantive requirement so Role B knows its footing:

- **`[PUB]`** — public API / published standard (Apple docs, POSIX, the IFF spec). Free
  to implement directly.
- **`[AROS]`** — an in-tree AROS header/driver (APL/LGPL). Cite the path; ours to use.
- **`[OURS]`** — this project's own spikes (`hosted/*`, `graft/*`, the H-series).
- **`[REF-CONFIRM]`** — a GPL reference *confirmed the approach is viable or surfaced
  that a problem exists*, but the requirement is restated here with an independent
  `[PUB]`/`[AROS]`/`[OURS]` justification. Role B implements from that justification,
  **not** from the reference. If you can't attach an independent justification, cut it.

Role B may rely on `[PUB]`/`[AROS]`/`[OURS]` freely. `[REF-CONFIRM]` items must each
stand on their independent justification alone.

## Licence map (what's clean-room vs. adoptable)

| Source | Licence | How we may use it |
|--------|---------|-------------------|
| UAE family (WinUAE/FS-UAE/Amiberry/E-UAE) | GPL-2.0 | **Clean-room only.** Role A reads; Role B never does. Reference for *shape*, never code. |
| vAmiga | GPL-2.0 | **Clean-room only.** Same. |
| **Emu68** | **MPL-2.0** | **Adoptable as isolated files** (file-level copyleft): keep its files verbatim with their MPL header in a quarantined dir, don't intermix with AROS source. Clean-room *not* required, but a clean integration boundary is. See `68k-jit/design.md` `[J0]`. |
| AROS tree | APL / LGPL-2.1 | Ours. Cite and build on directly. |
| Apple frameworks | proprietary, public API | Link against; implement to the published interface. |
| **vbcc / vasm / vlink** (Barthelmann/Wille) | free for non-commercial **+ explicit commercial exception for M68k/AmigaOS targets** (vbcc/vasm); vlink freeware | **Build TOOLS, not emulators — not GPL/MPL emulator source.** Built from source on this Mac (`hosted/jit68k/apps68k/tools/{build-vasm,build-vbcc}.sh`, binaries gitignored under `.toolchain/`), used to COMPILE the `[J5m]` C test program into a 68k AmigaOS hunk executable (`vbcc → vasm → vlink`). No emulator/decoder source is read or copied; these are a C compiler + assembler + linker. License text captured in `.toolchain/{VBCC,VLINK}-LICENSE.txt`. |

## Spec status

| Spec | Feature | Clean-room driver (Role A reads) | Status |
|------|---------|----------------------------------|--------|
| [cocoa-metal-display/spec.md](cocoa-metal-display/spec.md) | Metal display | vAmiga (Metal present path) | **`[D1]`/`[D2]`/`[D]` host shim GREEN** (`hosted/cocoametal/`, review-fixed: render-pass present, scalable, shader stage) · AROS binding next |
| [bsdsocket-net/spec.md](bsdsocket-net/spec.md) | Socket WaitSelect↔Signal pump | UAE `bsdsocket.cpp` pump | **`[N]` host pump GREEN** (`hosted/bsdsocket/`, review-fixed) · AROS binding next |
| [coreaudio-audio/spec.md](coreaudio-audio/spec.md) | CoreAudio SPSC ring + AHI sub-driver | FS-UAE/WinUAE audio (ring sizing + RT hand-off shape only) | **`[A]` host ring GREEN** (`hosted/coreaudio/`) · AROS AHI sub-driver next |
| [clipboard-bridge/spec.md](clipboard-bridge/spec.md) | IFF-FTXT + transcode + changeCount | WinUAE/Amiberry clip | **`[C]` host shim GREEN** (`hosted/clipboard/`, review-fixed) · AROS sync task next |
| [host-volume/spec.md](host-volume/spec.md) | Sidecar metadata + normalization | UAE directory-HD | **`[V]` host glue GREEN** (`hosted/hostvolume/`, review-fixed) · emul-handler overlay next |
| [68k-jit/spec.md](68k-jit/spec.md) | translator integration | Emu68 (MPL — adopted, not clean-roomed) | **`[J0]`→adapt Emu68 · `[J1]` MAP_JIT exec-memory GREEN · `[J2]` Emu68 emitter hosted GREEN · `[J3]` 68k→native LVO-call bridge GREEN · `[J4]` load→relocate→run chain GREEN** (`hosted/jit68k/`, real big-endian hunk binary parsed by OUR minimal loader + `HUNK_RELOC32` relocator grounded against `doshunks.h` + `internalloadseg_aos.c:292-332` — BE32-read/add-target-base/BE32-write — into a 32-bit sandbox; entry translated via the adopted Emu68 emitter + run under MAP_JIT; relocated pointer byte-exact + `d0` value-asserted incl. a skip-reloc negative control; AAPCS64 callee-saved x19..x26 preserve fix; quarantine `emu68/` + NOTICE) · **`[J5a]` memory load/store + sandbox-pointer boundary GREEN** (`hosted/jit68k/j5a_*`, block `move.l (a0),d0 ; addq #1,d0 ; move.l d0,(a0) ; rts` translated via the adopted Emu68 *emitter* with a HAND-ROLLED EA path: `An`→host `host_mem−origin + An` UXTW, unsigned bounds-check `(An−origin)>u(size−4)` → clean fault on OOB, big-endian `REV`; registers AND sandbox memory byte-exact vs an independent interpreter; skip-store / wrong-endianness / out-of-range negative controls all bite. **RA/EA adoption finding: Emu68's `M68k_EA.c` + `RegisterAllocator64.c` do NOT lift incrementally** — `An`-is-host-pointer 1:1-MMU (`M68k_EA.c:635-639`), ext words via the `ICACHE` software cache (`:760+`), SR/CTX in EL0 system registers (`RegisterAllocator64.c:175-185,288-303`); so the register allocator is OURS around the emitter, and **no new Emu68 file vendored** — Exhibit-B unchanged/clean) · **`[J5b]` control flow — a real loop with a conditional backward branch + genuine condition codes GREEN** (`hosted/jit68k/j5b_*`, loop `moveq #0,d0 ; moveq #5,d1 ; L: add.l d1,d0 ; subq.l #1,d1 ; bne.s L ; rts` → `d0=15` over 5 iterations, `d1=0`, CCR=0x04 Z-set, translated via the adopted Emu68 *emitter* into a SINGLE `jit_region` with an INTERNAL backward branch. **Real CCR:** `subq.l` → `subs` (flag-setting); the `bne.s` is an AArch64 `b.ne` (Z==0) reading the live NZCV straight from the `subs`; the full 68k N/Z/V/C/X is also recomputed into the CCR with non-flag-setting ops — `cset`/`orr`/`str` — emitted between the `subs` and the `b.ne` so adjacency holds (68k subtract C = AArch64 carry-CLEAR borrow; a 0−1 borrow cross-check asserts CCR=0x19=N\|C\|X). Single-region backward branch: loop-top is a recorded output-word index, the `b.ne` offset is negative; registers + full CCR + iteration count + termination byte-exact vs an independent interpreter; a broken-branch always-taken negative control hangs and is caught by a forked-child watchdog. **No new Emu68 file vendored — only existing `A64.h` encoders — Exhibit-B unchanged/clean**) · **`[J5c]` RE-HOST Emu68's REAL decoder + register allocator GREEN** (`hosted/jit68k/j5c_*`, the make-or-break experiment: DRIVE Emu68's REAL per-opcode decoders — vendored verbatim `emu68/M68k_LINE{8,9,B,C,D}.c` + `M68k_MOVE.c` + `M68k_MULDIV.c` + `M68k_EA.c` — behind hosted replacements for the three `[J5a]` couplings, to translate a richer block `moveq #-5,d2 ; add.l d2,d0 ; sub.l d3,d0 ; and.l d4,d0 ; or.l d5,d0 ; eor.l d6,d1 ; muls.w d7,d1 ; cmp.l d1,d0 ; and.l d2,d2 ; rts` — 8 opcodes across 6 real LINE decoders + REAL `moveq` sign-extend (`d2=0xFFFFFFFB`) + REAL condition-code derivation, all `d0..d7`/CCR byte-exact vs an independent interpreter, corrupt-decode negative control bites. **HOOK 2** `cache_read_16` = big-endian fetch from the host stream (no `cache.c`; splices back the 32-bit-truncated host pointer — a deeper fetch coupling). **HOOK 3** OUR `j5c_ra.c` register allocator implementing the `RA_*` interface with a MEMORY-BACKED CCR (`ldr/str` from the state struct, NOT `mrs/msr TPIDR_EL0`). **HOOK 1** sandbox/EA: register-direct opcodes drive the REAL EA cleanly; the memory-EA modes stay blocked. **VERDICT: re-hosting WORKS for the register/ALU/control class** — broad coverage of it = vendor more `M68k_LINE*.c` + extend the oracle; the memory-EA modes remain blocked by EA's no-sandbox-base + big-endian-CPU emit (Emu68 sets `SCTLR.EE` — edit `M68k_EA.c`'s ~6 load/store sites, the `[J5a]` logic). FOURTH coupling found: GNU `__attribute__((alias))` decoders are not byte-portable to clang/Mach-O — `emu68_darwinize.pl` (OURS) rewrites alias chains to plain forwarders in build-dir copies, quarantine stays byte-verbatim. New Emu68 files vendored: M68k_EA/LINE8/9/B/C/D/MOVE/MULDIV + headers — **Exhibit-B re-run clean**) · **`[J5d]` the WHOLE apps68k corpus through the JIT GREEN** (`hosted/jit68k/j5d_*` + the apps68k runner: a little engine — a per-basic-block translator DRIVING the REAL Emu68 decoders for every ALU/move/memory opcode + OUR re-hosted dispatcher ("MainLoop") owning inter-block control flow, the `(An)` sandbox-memory EA, and the `jsr`-through-vector → `[J3]` bridge. All FOUR real vasm-assembled hunk binaries run through the real-decoder JIT, each register file + sandbox memory byte-exact vs an INDEPENDENT from-scratch interpreter (`j5d_interp.c`, OURS, no Emu68): **mul=42**, **fact=120** (reg-to-reg `move.l` + `cmp.l` + nested loops), **arraysum=150** (relocated `lea` DATA + `add.l (a0)+,d0` through the REAL EA decoder — 2 sandbox accesses), **libcall=0** (`AllocMem`/`PutChar`/`FreeMem` via `jsr -off(a6)` decoded from the stream → the REAL `[J3]` marshaller into recording stubs, args/returns asserted). Corrupt-decode negative control bites; watchdog 15–20s → FAIL. **Vendored verbatim:** `M68k_LINE5.c` (ADDQ/SUBQ) + `M68k_CC.c` (link-only) — Exhibit-B re-grep clean. **The `[J5a]`/`M68k_EA.c` edit, realised as a build-dir transform:** `emu68_darwinize.pl --ea-sandbox` rewrites each `(An)`-class load/store in the BUILD-DIR copy to OUR `j5d_ea_mem` (sandbox-base add + `REV` byteswap + post/pre index); the quarantine `M68k_EA.c` stays BYTE-VERBATIM. **Dispatcher owns control flow:** rather than re-host LINE4/6's REG_PC/branch-target funnel, the engine decodes `Bcc`/`BRA`/`JSR`/`LEA`/`RTS` itself — Emu68's own block-exits-to-MainLoop model, re-hosted as a per-PC block cache + C dispatcher) · **`[J5e]` block-scoped register allocator (the "optimize" pass) GREEN** (`hosted/jit68k/j5e_test.c` + `j5c_ra.c` liveness + `j5d_engine.c` minimal frame: the REAL Emu68 decoders already keep the 68k Dn/An in fixed host regs across a block — D0..D7=w19..w26, An=w13..w17,w27..w29, no per-op spill inside the decoders — but the naive `[J5d]` frame bracketed EVERY block with a FIXED prologue/epilogue loading all 16 Dn/An + storing all 16 back unconditionally, 32 state ldr/str per block. The OURS RA now records, as the decoders call `RA_MapM68kRegister`/`RA_MapM68kRegisterForWrite`/`RA_SetDirtyM68kRegister`, which regs are READ-before-written (live-in) and WRITTEN (dirty); the engine emits the decoder body FIRST, then loads ONLY live-in regs in the prologue + stores back ONLY dirty regs in the epilogue. Read-before-write is the correctness point: a full `.L` write (`moveq`/`move.l #imm`) is NOT live-in so its prologue load is skipped, while a partial `.W`/`.B` write reads the reg first (`bfi`) so it is live-in AND dirty. **SPILL POLICY** at boundaries: every block exit (RTS/branch/the `jsr`→`[J3]` library bridge) stores dirty regs to the state struct BEFORE the boundary, so the bridge marshals from a fully-consistent memory state and `(An)` accesses (which go through the sandbox EA helper, not the state struct) don't race the deferred file. **MEASURED (byte-exact corpus gates the marker, not the smaller count):** AArch64 words 918→451 (−51%), state-struct ldr/str 512→45 (−91%) across mul/fact/arraysum/libcall; each program byte-exact vs the independent interpreter, negative control still bites. **No new Emu68 file vendored — the RA is OURS — Exhibit-B unchanged/clean.** Deferred: cross-block reg caching + linear-scan spilling under register pressure) · **`[J5f]` PC-driven dispatcher + a REAL 68k return stack + a block cache GREEN** (`hosted/jit68k/j5f_test.c` + the generalised dispatcher in `j5d_engine.c` + the SP/stack/control-flow model in `j5d_interp.c` + the new program `apps68k/sumsq.s`→`bin/sumsq.exe`, also wired into the apps68k runner: generalises the `[J5d]`/`[J5e]` flat-PC engine toward real subroutine-structured programs. The dispatcher becomes a PC-driven loop with a genuine 68k RETURN STACK — `a7` is a sandbox address; `bsr`/`jsr abs.l`/`jsr (An)` PUSH the 4-byte return address BIG-ENDIAN to `(a7-4)` then `a7-=4` (predecrement push), `rts` pops big-endian at `(a7)` then `a7+=4`, a `rts` back at the initial SP = top-level exit — plus computed `jmp(An)`/`jsr(An)` (target from a register) and the full `Bcc`/`BRA`/`BSR` `.B`/`.W`/`.L` displacement widths + `lea (d16,pc),An`, all DECODED from the stream at the dispatcher level (OURS — these are the LINE4/6 control-flow ops whose Emu68 decoders are entangled with the translator funnel our dispatcher *is*). The new program **sumsq** (sum of squares 1..5 = 55 via a `square` subroutine that nests a `mul` helper 2-deep, called from a loop + once via a COMPUTED `jsr (a0)`) runs through the REAL Emu68 decoders to **d0=55**, **byte-exact** vs the independent interpreter (extended to model the same SP/stack) on the full register file (incl. `a7` back at the initial SP `0x250000`) AND the **sandbox memory INCLUDING THE RETURN STACK**; telemetry pushed==popped==12, max nest depth 2, 1 computed jump; **BLOCK CACHE**: 8 blocks translated (misses) for 31 executions = **23 re-translations avoided**. Negative controls bite: a corrupt subroutine arg (`move.l d6,d0`→`d5,d0`) → `d0=0≠55`; a wild computed `jmp(a1)` (a1=0, out of sandbox) → caught cleanly by the sandbox bound, no host crash. `[J1]`–`[J5e]`+`apps68k` re-confirmed green (they now assert `a7` too — both engine and interpreter seed it identically). **No new Emu68 file vendored — the return stack/branch decode/computed jumps are dispatcher-level C in OUR files — Exhibit-B unchanged/clean**) · **`[J5g]` BROADEN the ISA + addressing-mode coverage GREEN** (`hosted/jit68k/j5g_test.c` + `j5g_shims.c` + the new program `apps68k/bubsort.s`→`bin/bubsort.exe`, also wired into the apps68k runner: VENDORS three MORE real Emu68 decoders BYTE-VERBATIM and drives them — `M68k_LINE0.c` (immediates/bit ops ori/andi/eori/subi/addi/cmpi/btst), `M68k_LINE4.c` (misc clr/neg/not/tst/ext/swap/lea/pea/movem), `M68k_LINEE.c` (shifts/rotates asl/asr/lsl/lsr/rol/ror) — plus the full M68000 addressing modes. The engine dispatcher routes groups 0/1/3/4/E to `EMIT_line0`/`EMIT_move`(.b/.w)/`EMIT_line4`/`EMIT_lineE`. **EA broadening:** `emu68_darwinize.pl --ea-sandbox` now also rewrites the FOUR EA funnel helpers (`load`/`store_reg_from`/`to_addr[_offset]`) in the BUILD-DIR copy of `M68k_EA.c` to OUR `j5d_ea_addr_offset`/`j5d_ea_addr_index` (`j5d_ea_helpers.c`), so `(d16,An)`, `(d8,An,Xn)`, abs.w, `(d16,PC)`/`(d8,PC,Xn)` all route through the sandbox EA (base-adjust + `REV` + index/scale; size-0=LEA address-compute); abs.l is an Emu68 inconsistency (a DIRECT `ldr_offset(tmp_reg,...)` not via the funnels) so the direct-site rewrite is extended to match `tmp_reg` too — the DECODE stays the REAL decoder, only the memory touch is sandboxed; quarantine `M68k_EA.c` stays BYTE-VERBATIM. All five new memory modes are verified byte-exact in `j5g_test.c`'s `ea-modes` sub-test (abs.l + (d16,An) loads + a (d8,An,Xn) store) on top of bubsort's (d8,An,Xn)+(An). **The demanding program** bubsort (bubble-sort {17,3,42,8,99,23} in place via `(0,a0,Dn.L)` indexed load/store → {3,8,17,23,42,99}, then a checksum/mixer folding the sorted array with the full shift/rotate + immediate set → **d0=0x00F5B9F5**) runs through the REAL decoders **byte-exact** vs the independent from-scratch interpreter (`j5d_interp.c`, extended to every new opcode/mode/size) on the FULL register file AND the sandbox memory (incl. the in-place sorted array) AND the **full CCR**; the negative control flips the sort branch (`ble.s`→`bgt.s`) so the array sorts wrong and the checksum diverges. **A FOURTH bare-metal coupling found + fixed:** Emu68's decoders hardcode **w0 as a 1-shot flag-extraction scratch** (`cset(0,...);bfi(cc,0,...)` — EMIT_BTST/ASx — bare-metal the CTX is in `TPIDRRO_EL0` so w0 is free); the `[J5c]`/`[J5d]` re-host had overloaded w0 as the state pointer (latent — the register/ALU corpus never hit those sites). Fixed by keeping the state pointer in **x1** (the decoders never hardcode w1/w2/w3), freeing w0 as Emu68 expects (`j5c_ra.c` + the engine + `j5c_build.c` prologue/epilogue). **A CCR-layout finding:** Emu68's internal CCR SWAPS C and V (bit0=V, bit1=C, bit2=Z, bit3=N, bit4=X — `SRB_Calt`/`SRB_Valt` + `bfi(cc,..,1,1)` for C in `A64.h`); latent because the earlier corpus only consumed Z/N, surfaced by `[J5g]`'s `ble`/`blt`/`bcc` branches — the dispatcher's `bcc_taken` + the oracle now both use Emu68's actual layout. **Vendored verbatim:** `M68k_LINE0/LINE4/LINEE.c` — diff vs upstream `305f686` empty, **Exhibit-B re-grep clean**, NOTICE updated; `j5g_shims.c` (OURS) provides link stubs for the un-driven LINE4 sub-ops (debug/disasm trace gate + `M68K_PopReturnAddress`, never on a hot path). `[J1]`–`[J5f]` + `apps68k` (now SIX programs) re-confirmed green. Honest deferrals: `neg.l`/`not.l` are NOT in the verified program (their X-bit in a long block is the deferred "X multi-precision chain" — not byte-exact vs the textbook 68k X yet); ROXL/ROXR, movem/movep/bitfield/BCD, the SR/exception model, SMC, FPU/privileged/line-A/F, and the boot-gated real AROS library env remain) · **`[J5h]` the X-bit MULTI-PRECISION chain GREEN** (`hosted/jit68k/j5h_test.c` + the oracle `j5d_interp.c` extended with `addx`/`subx`/`negx` + the multi-precision Z rule + `neg` generalised to all sizes, + the new program `apps68k/mp64.s`→`bin/mp64.exe`: CLOSES the `[J5g]` X-bit deferral. **RESOLVED SEMANTICS (empirically ground, PRM-cited):** each register-direct X-chain op was run through the REAL Emu68 decoders and the result + CCR byte compared against the M68000 PRM (4th ed., the ADDX/SUBX/NEGX/NEG/NOT flag rows) by hand — Emu68 is byte-exact CORRECT real 68k for the register-direct path (.l/.w/.b): `X=C=`carry/borrow out of the sized MSB; the **multi-precision Z rule** "Z cleared if nonzero, UNCHANGED otherwise" (Emu68 emits clear-only `b.eq +2 ; bic Z`, never an unconditional set — so Z ANDs across the words of a multi-precision value); `not` leaves X UNTOUCHED (its `EMIT_NOT` declares `SR_dirty=NZVC`, never X). **The `[J5g]` deferral was conservative — the ops were UN-ORACLED, not proven wrong.** **No new Emu68 file vendored:** `addx`(LINED)/`subx`(LINE9)/`negx`/`neg`/`not`(LINE4) live in the already-driven, already-vendored verbatim decoders — **Exhibit-B UNCHANGED/clean**. The program **mp64** does a 64-bit ADD (`add.l`+`addx.l`, X carries lo→hi) and a 64-bit NEGATE (`neg.l`+`negx.l`, X borrows lo→hi) → S=0x00000004_00000000, N=−S=0xFFFFFFFC_00000000, d0=0x000004FC, **byte-exact** vs the independent oracle on the full register file AND the CCR byte INCLUDING X AND the sandbox memory; a `subx.l`-driven 64-bit subtract sub-test (0−1 = 0xFFFFFFFF_FFFFFFFF) also byte-exact; the negative control flips `addx.l`→plain `add.l` (drops the X carry) so the high longword is off by one → the byte-exact value assert bites. `[J1]`–`[J5g]` + `apps68k` re-confirmed green. Still deferred: the `(An)`/`-(An)` MEMORY-EA forms of addx/subx/negx (Emu68's in-place memory `EMIT_NEGX` carries an upstream `// BROKEN!!!!` marker — scoped OUT), ROXL/ROXR, movem/movep/bitfield/BCD, the SR/exception model, SMC, FPU/privileged/line-A/F, and the boot-gated real AROS library env) · **`[J5i]` the 68k EXCEPTION / SR model GREEN** (`hosted/jit68k/j5i_test.c` + `apps68k/j5i.s`→`bin/j5i.exe` + the model in `j5d_engine.c` (engine) / `j5d_interp.c` (oracle) / `j5d_jit68k.h` (SR/vector/frame types): a bounded **dispatcher-level (C)** 68k exception model — a 256-longword vector table in the sandbox (VBR stand-in @ 0x00240000), the standard 68000 6-byte SR+PC short frame pushed to the supervisor stack `a7` (predecrement, big-endian, S set on entry), dispatch through the normal PC loop to the vector's handler, and `rte` (pop SR+PC, `a7+=6`, restore CCR+S, resume). Vectors covered, each from a REAL cause decoded straight from the stream: `TRAP #n`→32+n, `divu.w`/`divs.w` /0→5, `ILLEGAL`→4, out-of-sandbox/odd PC→bus/address 2/3 (reusing the `[J5a]` clean-fault detection, now a real 68k dispatch). The real hunk program installs handlers then raises trap #1 / divu.w #0 / ILLEGAL → exit d0=0x075A; three micro-tests cover the frame+`rte`-resume in isolation (saved PC asserted = post-trap insn, `rte` resumes there) and the bus-error path; each is **byte-exact** (registers + CCR/SR + sandbox memory + the per-exception frame log) vs the independent from-scratch oracle (`j5d_interp.c`, OURS, no Emu68); negative control (NOP the vector store) bites with a clean fault, NO host crash. **Architectural finding:** Emu68's `EMIT_Exception` branches into its bare-metal VBR path (`M68k_Exception.c`, NOT vendored — part of the runtime `[J0]` re-hosts; the symbol is a no-op stub in `j5c_shims.c`), so the faithful realisation is the spec's "68k exceptions handled in C" — the dispatcher owns it, the same funnel that owns the inter-block PC / return stack / `[J3]` bridge; the body opcodes still run through the REAL Emu68 decoders. **No `emu68/` file touched — Exhibit-B unchanged/clean; no new vendored file.** **The `graft/cpu_aarch64.h` integration seam stated:** an integrated JIT's host SIGSEGV in translated code routes through `graft/cpu_aarch64.h` SAVEREGS/RESTOREREGS at the AArch64 level; the 68k layer here is the pair — recover the m68k PC + fault kind, call the SAME `j5d_raise_exception` to build the 68k frame + vector (in this spike the sandbox bounds-check raises it WITHOUT a real host signal). Deferred: the real VBR register, USP/SSP split, 68010+ frame formats, group-0/1/2 priorities, and the actual host-SIGSEGV→this-path wiring (AROS-side integration). `[J1]`–`[J5h]` + `apps68k` re-confirmed green) · **`[J5j]` CAPSTONE — a SUBSTANTIAL recognisable real 68k program through the JIT GREEN** (`hosted/jit68k/j5j_test.c` + the new program `apps68k/mandel.s`→`bin/mandel.exe`: a fixed-point integer **Mandelbrot ASCII renderer** — three nested loops (row × col × iterate, 64×26 grid, maxiter 32, ~42k block executions), each iteration doing signed `muls.w` Q11 fixed-point multiplies + `asr.l` shifts + the full `add`/`sub`/`cmp` + `Bcc` set + `(d16,a5)` displacement-EA loads/stores in a scratch frame, plus a `PutChar` per cell + a newline per row through the `jsr -off(a6)` → `[J3]` LVO bridge. Runs through the REAL Emu68 decoders + OUR PC-driven dispatcher; the recognisable fractal (the main cardioid + period-2 bulb) is printed AND its **1690-byte PutChar OUTPUT STREAM** + final register file + full sandbox memory are asserted **byte-exact** vs the independent from-scratch interpreter (`j5d_interp.c`, OURS, no Emu68); the negative control (flip the escape compare's destination register `cmp.l #4,d0`→`#4,d1` in the JIT copy) makes the streams diverge so the assert bites. **THE GAP THE CAPSTONE SURFACED + CLOSED:** the immediate-source ALU forms `add.l`/`sub.l`/`cmp.l #imm,Dn` (LINED/LINEB encodings `0xD0BC`/`0x90BC`/`0xB0BC`, which vasm emits for `add.l #k,Dn`/`cmp.l #k,Dn` under `-no-opt`) were translated CORRECTLY by the JIT all along (the REAL `EMIT_lineD`/`EMIT_lineB` decoders handle the immediate EA) but were MISSING from the independent oracle (which only modeled the ADDI/CMPI LINE0 + register-source forms the earlier corpus used) — running mandel made the oracle stop on the unmodeled opcode. Fixed by adding those three forms to `j5d_interp.c` (flag rules identical to the register-source ops, same EMU68-internal C/V-swapped CCR layout); the byte-exact assert then verifies the REAL decoder against the extension, not a tautology. Two harness fixes the capstone also forced (NOT JIT bugs): the `[J3]` marshal-thunk was rebuilt per library call (the 8-region pool exhausted after 8 of ~1700 PutChar calls) → `stublib_dispatch` now caches one thunk per LVO (a real library base builds its JumpVec once); and the stub PutChar sink buffer was 256 bytes → enlarged to hold a full screen. **No new Emu68 file vendored** (the decoders are the already-vendored verbatim set) — Exhibit-B UNCHANGED/clean; the change is entirely in OUR files (the oracle + the program + the test + the stub harness). `[J1]`–`[J5i]` + `apps68k` re-confirmed green) · **`[J5k]` CROSS-REGION BLOCK CHAINING + CROSS-BLOCK REGISTER CACHING GREEN** (`hosted/jit68k/j5k_test.c` + the chaining engine in `j5d_engine.c` (OURS, below the seam) + the `j5d_stats` chaining fields in `j5d_jit68k.h`; the `[J5e]` test re-expressed onto the new frame): the perf-maturity step — chain cached blocks with **direct AArch64 branches past the C dispatcher** and keep the 68k register file **pinned live in host regs across the hop** instead of storing-then-reloading through `struct j5d_m68k_state`. **TWO-ENTRY BLOCKS + LAZY LINKING:** each block has an OUTER entry (the C function-pointer target — saves the callee frame incl. now `w13..w17`/A0–A4, loads ALL 16 Dn/An) and, after that full load, a CHAIN ENTRY (a chaining predecessor `b`-branches here with the file already live). A block whose terminator is a STATIC-TARGET transfer — fall-through/`BRA`/`jmp abs.l`/`Bcc` — emits a backpatchable tail branch (each slot initially a `b` to its own epilogue = the C return); the first time the edge is taken and the target exists, the dispatcher LAZILY LINKS the slot to a cross-region `b` into the target's chain entry (one word patched in a `pthread_jit` write window + i-cache invalidate). A `Bcc` emits an INLINE 68k condition eval (CCR from memory, Emu68-internal C/V-swapped layout) → cbz + two slots — **validated exhaustively against the dispatcher's `bcc_taken` for all 14 conditions × 32 CCR values** before wiring, so the inline decision and the C fallback always agree. `b` reaches ±128 MiB; out-of-range stays the fall-to-dispatcher default — **correctness is never reachability-gated.** NON-static terminators (`rts`/`bsr`/`jsr abs.l`/`jsr/jmp (An)`/the `jsr d16(A6)` `[J3]` bridge/TRAP/illegal/divu0/`rte`) are NOT chained — they stay C round-trips by design (the return stack, the bridge, and the `[J5i]` model live in the dispatcher); a chain A→…→Z re-enters C only at Z's epilogue, which records Z's cache index (baked immediate) in an engine global so the dispatcher decodes the TERMINAL block's terminator, not the head's. **SPILL POLICY (correctness-critical):** the file is NOT spilled across a chained hop (A's epilogue store-16 + B's prologue load-16 both skipped — the cross-block register caching), but the WHOLE 16-register file is stored at EVERY dispatcher exit (the universal epilogue) — load-bearing because a chain reaches exactly one epilogue and any upstream block may have written any reg; storing only the terminal block's dirty set would lose an upstream write when the `ldp` restores the head's saved callee regs. So `struct j5d_m68k_state` is memory-consistent at every boundary that needs it: `rts` (return-stack pop), the `[J3]` LVO bridge (marshals args from the memory state), an exception dispatch, a computed `(An)` jump, an unresolved link. The CCR syncs through memory at every block boundary (one byte). **MEASURED (byte-exact gates the marker, not speed):** the chain-heavy Mandelbrot renders byte-identically to the independent interpreter (1690-byte PutChar stream + final registers + full sandbox memory), `d0==0`, negative control bites — while **C-dispatcher round-trips collapse 41 757 → 1 728 (95.9 % fewer)**, replaced by **40 029 direct block→block chain branches** over **21 lazily-linked edges**, **eliding 1 280 928 register memory-ops** at the chained boundaries. **INVALIDATION:** no mid-run cache eviction exists, so a linked branch never dangles within a run; `j5d_run_free()` drops all regions+links between runs. **SMC / dirty-code-page invalidation DEFERRED** (we don't chain across self-rewriting code; the `M68K_VerifyUnit`/CRC32 re-host is the separate SMC pass — the per-block link table already records the patch sites an un-chaining would need). **No `emu68/` file touched — the chaining is dispatcher-level C in OUR files — Exhibit-B UNCHANGED/clean.** **The FROZEN SEAM is UNCHANGED (confirmed):** the `jit_region` API (chaining reuses the existing write-window/finalize), the `struct M68KState`/`j5d_m68k_state` field layout (chaining counters are engine globals + `j5d_stats` fields, never new state-struct fields), the `[J3]` LVO marshalling contract, and the `[J5i]` exception/SR model — all below them. `[J1]`–`[J5j]` + `apps68k` re-confirmed green) · **`[J5l]` movem (move-multiple-registers) GREEN** (`hosted/jit68k/j5l_test.c` + `j5d_ea_helpers.c` (the `j5d_movem_*` helpers) + `j5d_interp.c` (oracle) + `emu68_darwinize.pl --movem-sandbox` + the new program `apps68k/j5l.s`→`bin/j5l.exe`, also wired into the apps68k runner — now SEVEN programs: closes the highest-value remaining ISA gap — `movem` is **the opcode every compiler-generated 68k function uses in its prologue/epilogue** (`movem.l d2-d7/a2-a6,-(sp)` save + `movem.l (sp)+,d2-d7/a2-a6` restore), so it is **required to run real compiled Amiga code**. DRIVES Emu68's REAL `EMIT_MOVEM` decoder (the verbatim, already-vendored-at-`[J5g]` `M68k_LINE4.c`) — the predecrement **REVERSED** register-mask order (bit0=A7…bit15=D0, An predecremented before each store), the postincrement / predecrement An-update, the control / `(d16,An)` / abs forms, both `.l` and `.w` (the `.w` LOAD sign-extends word→long, the `.w` STORE writes the low 16 bits) — `movem` does not affect the CCR. **The memory routing (the one new mechanism):** `EMIT_MOVEM` does NOT use `M68k_EA.c`'s `ldr_offset(reg_An,…)` sites the `--ea-sandbox` transform rewrites — it resolves the EA base ONCE (size 0, so `base` holds the 68k ADDRESS) and emits its OWN inner loop of raw, often PAIRED, transfers straight off `base` (`stp`/`stp_preindex` 32-bit W-pair, `str`/`str_offset_preindex`, `strh`, `ldr`, `ldp`/`ldp_postindex`, `ldrsh`) — the 1:1-MMU + big-endian-CPU assumption. A SECOND darwinize pass (`--movem-sandbox`, on `M68k_LINE4.c`) rewrites exactly those movem memory sites in the BUILD-DIR copy to OUR `j5d_movem_*` helpers (`j5d_ea_helpers.c`): `host = base_adjust(x12) + (uint32_t)base` UXTW + a per-register `REV` byteswap, **decomposing the W-register pairs into per-register sandbox accesses** and **preserving the pre/post-index An update** (`stp_preindex`→`An-=N` first; `ldp_postindex`→`An+=N` last); the non-memory base arithmetic Emu68 emits (the `add_immed`/`sub_immed` bumping/snapshotting the 68k An) is left AS-IS. The patch is a call-substitution scoped to sites whose base operand is the literal `base` (uniquely movem; `lea`/`link`/`unlk`/`pea` use `dest`/`src`/`sp`) — the **QUARANTINE `M68k_LINE4.c` stays BYTE-VERBATIM** (`diff` vs upstream empty). **The demanding program** j5l: a compiler-style non-leaf subroutine `work` saves d2-d7/a2-a6 with a predecrement movem, CLOBBERS every one, restores them with a postincrement movem, returns — and the caller COUNTS the survivors → **d0 == 11** (the save/restore actually MATTERED), plus the control / `(d16,An)` / `.w` forms against a fixed sandbox frame; run through the REAL decoders **byte-exact** vs the independent interpreter (extended to model movem: both mask orders, the An update, `.w` sign-extend on load) on the FULL register file AND the WHOLE sandbox memory (incl. the predecrement save frame on the stack AND the control frame), with **34 movem memory accesses** through the JIT. **Negative control:** zero the EPILOGUE restore mask in the JIT copy → the clobbered regs leak, d0≠11 (JIT 0) while the oracle restores (d0==11) → the JIT diverges, so the byte-exact + survival asserts bite. **No new Emu68 file vendored** (`M68k_LINE4.c` was vendored at `[J5g]`) — **Exhibit-B UNCHANGED and re-grep clean**; no `emu68/` file touched. **The FROZEN SEAM is UNCHANGED (confirmed):** movem is decoder/EA coverage below the seam — the `jit_region` API, the `struct M68KState`/`j5d_m68k_state` field layout, the `[J3]` LVO contract, and the `[J5i]` exception/SR model are all untouched. The whole corpus (mul/fact/arraysum/libcall/sumsq/bubsort/mp64/mandel) re-run byte-exact; `[J1]`–`[J5k]` + `apps68k` re-confirmed green. Deferred: movem to/from an indexed `(d8,An,Xn)` or PC-relative EA (decoded by the REAL decoder but not in the verified program); `movep`) · **`[J5n]` the DIAGNOSTICS subsystem — faults are never silent GREEN** (`hosted/jit68k/{j5n_diag.{h,c}, j5n_symbols.{h,c}, j5n_test.c}` (all OURS, AROS-licensed, NO Emu68 source) + the fault fixtures `apps68k/{diagfault,diagill,diagbus}.s`→`.exe` + additive NULL-gated hooks in `j5d_engine.c` + an optional step hook in `j5d_interp.c`): on ANY fault, ONE funnel `j5d_fault(kind,detail,st,sb,host)` writes a self-contained, shareable **crash BUNDLE** (`jit68k-crash-<UTCstamp>-<faultkind>.tar.gz`) — `README.txt` (plain-English: what it is + the one action "send this .tar.gz" + a developer workflow), `MANIFEST.txt` (the precise index), `REPORT.txt` (the two-level dump: coordinate kind/detail/instruction-#N/PC/in-function/host-context, the faulting 68k instruction (opcode words + disasm), 68k regs D0-D7/A0-A7/PC/SR, host AArch64 regs x0-x30/sp/pc/cpsr from the `ucontext` (the `graft/cpu_aarch64.h` shape), BOTH stacks (the 68k call-stack walked from a7→symbols AND the native `backtrace()`), and a flight-recorder ring), `core.snapshot` (M68KState + the full raw sandbox image, reloadable — `j5n_snapshot_load` round-trips it), `program.exe`+`program.sha256` (a self-contained SHA-256; bundle digest == committed binary, verified), `REPRODUCE.txt` (the `run-to #N` replay command + `git rev-parse HEAD` + build config), and `diverge.txt` (differential mode only) — then prints a LOUD banner with the absolute path. **The host-signal safety net** (`SIGSEGV`/`SIGBUS`/`SIGILL`/`SIGFPE` on a `sigaltstack`) recovers the host `mcontext` + the current 68k state and routes to the funnel, then re-raises to die — never a silent host crash (a forked child does a real out-of-sandbox HOST access; asserted it died by the signal AND a bundle was written — the `graft/cpu_aarch64.h` seam, host-side). **HUNK_SYMBOL mapping:** `j5n_symbols.c` re-walks the same hunk file the loader consumed (the loader is UNTOUCHED — it still skips `HUNK_SYMBOL`/`HUNK_DEBUG`) into a PC→symbol map; `diagfault.exe` (assembled WITH symbols) makes the report name the faulting function `divide`. **The DIFFERENTIAL (lockstep) mode (`JIT68K_DIFF=1`):** `j5d_run_diff` runs the JIT and the independent interpreter oracle in lockstep and traps at the first instruction where their `M68KState` diverges (block-boundary detection + instruction-precise localization), emitting `diverge.txt` with both states — a runtime mistranslation locator; the test INJECTS a deterministic divergence (the JIT's bridge returns a different d0 than the oracle's — a negative-control-style forced mismatch) and asserts the trap lands at the exact diverging PC. **Deterministic coordinate + REPLAY-TO-N (`JIT68K_RUNTO=N`):** a global instruction counter numbers every 68k instruction; the bundle records the fault's #N, and replaying via the instruction-precise oracle breaks at exactly #N, landing on the crash (same PC + #N asserted). **The FROZEN SEAM is UNCHANGED (git-confirmed, no diff):** wired via a side-channel `j5d_set_diag()` that MIRRORS `j5d_set_exc_log()`, reads `M68KState` read-only, engine hooks additive + NULL-gated (diag==NULL = the zero-overhead path the whole corpus runs on; diagnostics off the hot path). Untouched: `jit_region.h`, the `struct M68KState`/`j5d_m68k_state` layout (`j5d_jit68k.h`), the `[J3]` LVO contract, the `[J5i]` exception model. **No `emu68/` file touched — Exhibit-B UNCHANGED/clean.** `[J1]`–`[J5m]` + `apps68k` re-confirmed green. Deferred: instruction-level (vs block-level) JIT lockstep (needs block-splitting); `HUNK_DEBUG` line→source mapping (symbols suffice); the production host-SIGSEGV→`j5d_raise_exception` wiring INSIDE AROS (owner's track) · **`[J5o]` the 68881/68882 FPU CORE GREEN** (vendored BYTE-VERBATIM from pin `305f686`: `emu68/{M68k_LINEF.c, math/libm.h, tlsf.h}` — `diff` vs upstream EMPTY, Exhibit-A MPL header on M68k_LINEF.c, **Exhibit-B re-grep clean**, NOTICE updated; OURS, NO Emu68 source: `hosted/jit68k/{j5o_fpu_shims.c, j5o_test.c}` + the FP single/double **and** integer-format sandbox helpers in `j5d_ea_helpers.c` + the FPU register file/FPCR/FPSR in `j5c_ra.c` + the FPU oracle in `j5d_interp.c` + the `--fpu-sandbox`/`--libm-asm-fix` darwinize passes + `apps68k/j5o.s`→`bin/j5o.exe` (assembled `-m68882`)): the FIRST corpus program to use the FPU coprocessor (line-F). Drives Emu68's **REAL** `EMIT_FPU` decoder (`M68k_LINEF.c`) — **FMOVE** reg↔reg, mem↔reg, with every **format conversion** (single `.s` & double `.d` memory↔FP through the sandbox byteswap; integer `.l`/`.w`/`.b`↔FP from Dn and memory), **FADD/FSUB/FMUL/FDIV/FSQRT/FABS/FNEG**, **FCMP/FTST** (set the FPSR condition byte N/Z/I/NAN, gated by Emu68's own `FPSR_Update_Needed` forward-scan) — asserted **BIT-EXACT** (the raw `double` bit patterns of FP0..FP7, so NaN/−0/inf compare by bits; the FPSR cc byte; the stored `.d/.s/.l/.w/.b` results; the integer regs; the whole sandbox memory; `d0`) vs an **INDEPENDENT C-`double` ORACLE** (`j5d_interp.c`, OURS, no Emu68, computes every op directly in C `double`), plus a THIRD fully-independent hand-check and a **negative control** (flip FADD→FSUB in the JIT copy only → JIT fp0=45 vs oracle fp0=95, the assert bites). The **sandbox re-hosting:** the `.s`/`.d` FP loads/stores route through OUR `j5d_fpu_f*` helpers, and the integer-format `.l/.w/.b` FMOVE↔memory — which Emu68 emits as PLAIN integer `ldr/str`(+h/+b,+ur/pre/post) straight off the resolved 68k address INSIDE `M68k_LINEF.c` (`FPU_FetchData`/`FPU_StoreData` Case 3), NOT via `M68k_EA.c` — route through OUR `j5d_fpu_i*` helpers (same base-adjust `x12` + `REV`/`REV16` byteswap, sign-correct for the decoder's `scvtf`/`fcvtzs`); `--fpu-sandbox` rewrites those sites in the BUILD-DIR copy only, the **QUARANTINE `M68k_LINEF.c` stays BYTE-VERBATIM** (`diff` empty). **PRECISION MODEL (documented, mirrors Emu68):** FP0..FP7 modeled in IEEE-754 binary64 (`double`) — AArch64 has no 80-bit FP — so this is COMPLETE *instruction* coverage at double precision; 80-bit extended-precision exactness is **NOT bit-reproducible** on AArch64 (for these IEEE-defined ops the double results are deterministic/bit-exact, the gate). FP0..FP7 → AArch64 **d8..d15** (callee-saved). **THE SEAM IS FROZEN EXCEPT AN APPEND (confirmed):** the FP register file (`double fp[8]`, `uint32_t fpcr`, `uint32_t fpsr`) is **APPENDED AFTER all existing `struct j5d_m68k_state` fields** (exactly as `sr_high`/`exc_count` at `[J5i]`) so **every existing offset is byte-for-byte UNCHANGED** — verified `d`=0/`a`=32/`ccr`=64/`pc`=68/`sr_high`=72/`exc_count`=74 frozen; `fp`=80/`fpcr`=144/`fpsr`=148 new; the engine emits the FP load/store offsets with `offsetof()` (static-asserted `fpcr==144`/`fpsr==148`). The rest of the seam — `jit_region.h`, the `[J3]` LVO contract, the `[J5i]` exception/SR model — is untouched; the engine's `EMIT_lineF` is a WEAK fallback so the integer-only spikes (J5d..J5n) that do not vendor `M68k_LINEF.c` still link (the strong vendored decoder overrides it in the J5o build). The whole corpus (mul/fact/arraysum/libcall/sumsq/bubsort/mp64/j5l/mandel) re-run byte-exact through the same FPU-enabled engine; `[J1]`–`[J5n]` re-confirmed green. **Deferred to the NEXT FP increment:** the transcendentals (FSIN/FCOS/FETOX/FLOGN/… — their math-helper sites are abort-on-call stubs) — **NOW DONE in `[J5p]`**; FBcc/FScc/FDBcc/FTRAPcc, FMOVEM, the 80-bit `.x` + packed `.p` MEMORY formats, and FP exceptions (the FPSR exception/accrued bits + FP traps) · **`[J5p]` the 68881/68882 TRANSCENDENTAL + FP-UTILITY ops GREEN** (NO new vendored file — the SAME byte-verbatim `emu68/M68k_LINEF.c` from pin `305f686`, Exhibit-B re-grep UNCHANGED/clean; OURS, NO Emu68 source: `hosted/jit68k/j5p_test.c` + the transcendental routing & REAL `EMIT_SaveRegFrame`/`EMIT_RestoreRegFrame` in `j5o_fpu_shims.c` + the transcendental/utility opmodes & corrected FPSR-cc model in `j5d_interp.c` + the `--fpu-sandbox` (C) call-site-address-LE-fix and (D) FMOD GP-scratch-fix in `emu68_darwinize.pl` + the Emu68 post-emit-marker strip in `j5d_engine.c` + `apps68k/j5p.s`→`bin/j5p.exe`, `-m68882`): drives Emu68's **REAL** `EMIT_FPU` decoder for the 68881 transcendentals — `FSIN`→sin `FCOS`→cos `FTAN`→tan `FASIN`→asin `FACOS`→acos `FATAN`→atan, `FSINH`→sinh `FCOSH`→cosh `FTANH`→tanh `FATANH`→atanh, `FETOX`→exp `FETOXM1`→expm1 `FTWOTOX`→exp2 `FTENTOX`→exp10, `FLOGN`→log `FLOGNP1`→log1p `FLOG10`→log10 `FLOG2`→log2, `FSINCOS`→sin+cos — and the FP-utility ops `FINT`(frint64x/rint) `FINTRZ`(frint64z/trunc) `FGETEXP` `FGETMAN` `FMOD` `FREM`(remquo/remainder) `FSCALE`(ldexp). **THE ROUTING (no quarantine edit — the load-bearing realization):** the 68881 transcendentals are implementation-defined in their last ULPs, so the decoder's helper sites are routed to the **host `libm`** and the oracle uses the **SAME** host `libm` — bit-exact verifies the TRANSLATION (decode + register-as-argument + store), not a re-derivation of sin(). The verbatim decoder bakes `&sin/&cos/…` in at translate time, so routing is just NOT shadowing the standard `libm` names in `j5o_fpu_shims.c` (link resolves to the system `libm`), defining only `exp10`/`sincos`(→`struct double2`)/`remquo`(→`struct rq`) as thin wrappers. Three OURS fixes (quarantine stays byte-verbatim): REAL Save/RestoreRegFrame push x1(`M68KState*`)/x12(base-adjust)/REG_PROTECT(A0–A4,PC) around the `blr` (the `blr` clobbers AAPCS64 caller-saved regs; the 68k integer file x19–x28 + FP file d8–d15 are callee-saved so `libm` preserves them); the (C) transform corrects the helper-address `movz/movk` byte-order (the verbatim sequence is word-reversed on a little-endian host — works on Emu68's RasPi only because helpers sit in low memory; 31 sites); the (D) transform moves FMOD's GP scratch off x1 (Emu68 uses x0/x1 as free scratch bare-metal, but x1 is OUR permanent state ptr — FMOD corrupted it). The engine also strips Emu68's post-emit optimizer markers (`0xfffffff0` etc.) the FP decoder leaves in the stream (its optimizer, NOT lifted, would remove them). Asserted **BIT-EXACT** (FP0..FP7 raw `double` bits — NaN/−0/inf by bits — the FPSR cc byte, the FP-to-memory stored doubles, the integer regs, the whole sandbox, `d0`) vs the INDEPENDENT C-`double` oracle, + a host-`libm` hand-check (27 transcendental/utility results + 3 NaN edges) + NaN FPSR edges (`FACOS(10)`/`FLOGN(-1)`/`FATANH(10)`→NaN, NAN bit set) + a FSIN→FCOS (opmode 0x0e→0x1d) negative control (JIT stores cos(0.5) where the oracle stores sin(0.5) → diverges). **FPSR-cc CORRECTION:** the `[J5o]` oracle latently mapped the AArch64 C flag → the 68881 I (infinity) bit, but `EMIT_GetFPUFlags` (A64.h) `bic`s C out before the shift so **I is NEVER set** — verified empirically against the JIT (FTST: −2.0→cc 0x08 N, 0.0→0x04 Z, NaN→0x01 NAN, +inf→0x00, −inf→0x08); the discrepancy never surfaced in `[J5o]` (every case had C=0 in the final FPSR-setter), `[J5p]`'s NaN edge (C=1) exposed + fixed it. **PRECISION:** results are host-`libm` IEEE-double, the reference both sides use (real 68881 used 80-bit + its own polynomials, not bit-reproducible on AArch64 — consistent with the `[J5o]` model). **THE FROZEN SEAM IS UNCHANGED (confirmed):** `[J5p]` adds NO state field (`fpcr`==144/`fpsr`==148 static-asserts hold), only routes existing helper sites + extends the oracle/transform; `jit_region.h`, the `struct M68KState`/`j5d_m68k_state` layout, the `[J3]` LVO contract, the `[J5i]` exception model — all untouched. The whole corpus + the `[J5o]` FP core re-run byte-exact; `[J1]`–`[J5o]` re-confirmed green. **Still deferred:** FBcc/FScc/FDBcc/FTRAPcc — **NOW DONE in `[J5q]`**; FMOVEM, the 80-bit `.x` / packed `.p` MEMORY formats, FP exceptions (the integer FPSR N/Z/Inf-never-set/NAN cc byte is live; the FMOD/FREM quotient byte is computed but not asserted) · **`[J5q]` the 68881/68882 FP CONDITIONAL CONTROL-FLOW family (FBcc/FScc/FDBcc/FTRAPcc) GREEN** (NO new vendored file, NO new darwinize pass, Emu68 quarantine BYTE-VERBATIM; OURS, NO Emu68 source: `hosted/jit68k/j5q_test.c` + the shared FP predicate `j5q_fp_cond_taken()` & the FPSR cc constants & the vector-7 constant in `j5d_jit68k.h` + the four ops in the dispatcher `j5d_engine.c` and the oracle `j5d_interp.c` + the `fp_cc_ops` telemetry field + `apps68k/j5q.s`→`bin/j5q.exe`, `-m68882`): builds on the FPSR cc byte `[J5o]`/`[J5p]` made live. **FBcc** (.W/.L branch), **FScc** (set byte true/false → Dn or memory), **FDBcc** (decrement-and-branch loop), **FTRAPcc** (trap-on-condition → the `[J5i]` exception path, vector 7) read that FPSR cc and act on the 68881 FP predicate. **Decoded at the DISPATCHER level in C, NOT through Emu68's decoder** (the architectural fit, like the integer Bcc): Emu68's verbatim `M68k_LINEF.c` emits FBcc/FScc through its bare-metal `REG_PC` branch funnel (`csel REG_PC` + the embedded `tmpptr/branch_target/0xfffffffe` sentinel) — the machine-owning model the `[J5f]` dispatcher re-hosts — and FDBcc/FTRAPcc have NO decoder body at all; so all four are added to `is_terminator()` (the body loop stops before them) and decoded in the dispatcher's C terminator loop, reading `st->fpsr` (memory-backed, flushed by the preceding FCMP/FTST whose `FPSR_Update_Needed` forward-scan already recognises these consumers). **THE PREDICATE TABLE (`j5q_fp_cond_taken`, shared by BOTH the dispatcher and the oracle):** the 6-bit selector picks 1 of 32 predicates; bit5 (the IEEE-aware/SIGNALLING variant) does NOT change the truth — only whether an unordered operand raises BSUN (an FP trap, deferred) — so the truth table is over the low 4 bits (16 base conditions), a boolean of FPSR {N(bit27), Z(bit26), NAN(bit24)} (I/infinity is never set). OUR re-derivation of the table Emu68's verbatim FBcc decoder emits in AArch64 (`tst`/`orr`/`eor` vs `FPSR_N`/`FPSR_Z`/`FPSR_NAN`), evaluated in C; grounded against the M68000 FPU conditional-predicate table. **The IEEE UNORDERED (NaN) cases are the load-bearing part — the ORDERED predicates (OGT/OGE/OLT/OLE/OGL/OR) are FALSE on a NaN operand, the UNORDERED (UN/UEQ/UGT/UGE/ULT/ULE) are TRUE.** Verified BYTE-EXACT (integer + FP regs + FPSR cc + the WHOLE sandbox — the FScc-stored bytes, the FDBcc count, the FTRAPcc frame — + the exit `d0 == 0x000103FF`) + correct control flow vs the INDEPENDENT oracle (`j5d_interp.c`, same predicate fn), with an independent hand-check (FScc bytes / FDBcc count / ordered-vs-unordered NaN paths / FTRAPcc handler) and a negative control (FBOR→FBUN in the JIT copy only → on the NaN FTST the JIT takes the unordered path `d0=-1` where the oracle stays ordered `d0=0x103FF` → diverged). `fp_cc_ops=21`, FTRAPcc→vector7 fired=1. **THE SEAM IS UNCHANGED:** NO state field added (the predicate + the FPSR cc constants + the vector-7 constant + the shared `j5q_fp_cond_taken()` are header-only; `fp_cc_ops` is appended to the `j5d_stats` TELEMETRY struct, not `struct M68KState` — `fpcr==144`/`fpsr==148` static-asserts hold); `jit_region`, the `[J3]` LVO contract, the `[J5i]` exception model untouched; no `emu68/` file touched — Exhibit-B UNCHANGED/clean. The whole corpus + the `[J5o]`/`[J5p]` FP programs re-run byte-exact; `[J1]`–`[J5p]` re-confirmed green. **Still deferred:** FMOVEM + FP system-register moves (FMOVE↔FPCR/FPSR/FPIAR), the 80-bit `.x` / packed `.p` MEMORY formats, FP exceptions (incl. BSUN — the trap the SIGNALLING predicate variants raise on an unordered operand; the predicate TRUTH is correct, the trap is not raised), then a vbcc-compiled FP capstone · NEXT (a *bigger* app): dirty-page SMC block-cache invalidation, the privileged ISA, the rest of the ISA/addressing modes, and the boot-gated real AROS library environment (LoadSeg→RunCommand/CallEntry + real libraries vs the stub exec) |

Specs are written one at a time, Role A → reviewed → handed to a fresh Role B agent. Host drivers
with a background host thread also conform to [host-wake-pattern.md](host-wake-pattern.md).

**Spike vs. production:** a GREEN host shim proves the mechanism in the unattended loop; it is
not yet the production contract. Each spec carries a "Spike status vs production contract" note
listing the open policy items (full Unicode tables, errno translation, AROS-side loop-break
tokens, etc.) that the AROS-side binding must still settle.
